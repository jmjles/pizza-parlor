import { IngredientCategory } from '@/assets/sampleData'

const Ingredients = (props: IngredientsProps) => {
    const {items} = props;
    return (
        <>

        </>
    )
}
export default Ingredients;
type IngredientsProps = {
    items:IngredientCategory[];
}